require 'spec_helper'

describe ProjectEz do
  it 'has a version number' do
    expect(ProjectEz::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
